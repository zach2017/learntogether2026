# ---------- Core service credentials (POC defaults) ----------
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
POSTGRES_DB=recruitment_db

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET=resumes

# Use a distinct key for production; this is for POC
JWT_SECRET=dev_secret_change_me
CORS_ORIGIN=http://localhost:8080

# Ollama models to pull at startup (comma-separated)
OLLAMA_MODELS=llama2,nomic-embed-text
# You can also try: llama3, codellama, all-minilm, mxbai-embed-large

# Nginx exposed port
NGINX_PORT=8080
MINIO_CONSOLE_PORT=9001
