server:
  port: 8081

spring:
  application:
    name: resource-server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/spring-realm
          jwk-set-uri: http://localhost:8080/realms/spring-realm/protocol/openid-connect/certs

logging:
  level:
    org.springframework.security: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/spring-realm
          jwk-set-uri: http://keycloak:8080/realms/spring-realm/protocol/openid-connect/certs
