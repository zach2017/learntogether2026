server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Proxy API calls to the api service inside the cluster
  location /api/ {
    proxy_pass http://api:8080/api/;
  }
}
